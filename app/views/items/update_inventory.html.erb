<h3><%= t('text.update')%> Inventario</h3>
<% if @items.size > 0 %>
	<%= form_for :item, url: {action: 'update_stock'} do %>
		<table class='table table-hover table-bordered table-condensed'>
			<thead>
				<tr>
					<th>Material</th>
					<th>En existencia</th>
					<th>Nueva cantidad</th>
				</tr>
			</thead>
			<tbody>
				<% @items.each do |item| %>
					<%= fields_for "item_ids[]", item do |item_fields| %>
						<tr>
							<%= item_fields.hidden_field :comment, value: 'Actualización de Inventario' %>
							<td><%= item.label %></td>
							<td><%= item.stock %></td>
							<td><%= item_fields.text_field :new_quantity, value: item.stock, class: 'span1 number' %></td>
						</tr>
					<% end %>
				<% end %>
			</tbody>
		</table>
		<%= render 'shared/paginate', object: @items %>
		<%= submit_tag "#{t('text.update')} Inventario" %>
	<% end %>
<% else %>
	<%= render 'shared/no_records_found', object_class: Item %>
<% end %>