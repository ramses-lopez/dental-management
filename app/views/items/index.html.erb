
<h3>Listado de <%= Item.model_name.human.pluralize(:es) %></h3>
<% if @items.size > 0 %>
	<table class='table table-condensed table-hover table-bordered'>
		<thead>
			<tr>
				<th>Descripción</th>
				<th>Existencia</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<% @items.each do |item| %>
				<tr>
					<td><%= item.label %></td>
					<td><%= item.stock %></td>
					<td><%= render 'shared/btn_group_action', object: item %> </td>
				</tr>
			<% end %>
		</tbody>
	</table>
	<div class='text-center'>
		<%= will_paginate %>
	</div>
<% else %>
	<%= render 'shared/no_records_found', object_class: Item %>
<% end %>
<br>
<%= link_to "#{t('text.new')} #{Item.model_name.human}", new_item_path, class: 'btn' %>